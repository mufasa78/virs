# Core dependencies (required for all versions)
numpy>=1.20.0
opencv-python>=4.5.3
pyyaml>=6.0
streamlit>=1.20.0

# Minimal dependencies (for fixed_app.py)
# No additional dependencies required

# Full dependencies (for streamlit_app.py)
torch>=2.6.0
torchvision>=0.17.0
matplotlib>=3.4.3
scikit-image>=0.18.3
tqdm>=4.62.3
einops>=0.4.1
timm>=0.5.4
lpips>=0.1.4

# Ensure pip is updated
pip>=24.0
